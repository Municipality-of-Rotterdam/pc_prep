name: pc_prep
channels:
  - conda-forge
  - anaconda
  - nodefaults
dependencies:
  - python=3.11
  - pip:
    - pc_prep==X.X.X
    - >-
      --extra-index-url
      https://pkgs.dev.azure.com/${org_name}/rdam/_packaging/${feed_view}/pypi/simple/
